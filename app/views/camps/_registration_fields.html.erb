  <div class="fields">
    <div class="min_registration">
      <!-- x icon to remove -->
      <%= f.link_to_remove("x") %>
      <!-- fields -->
      <%= f.input :student_id, collection: Student.active.alphabetical, prompt: "Select student.." %>
      <%= f.input :payment_status, collection: ['deposit', 'full'] %>
      <%= f.input :points_earned %>
    </div>
  </div>